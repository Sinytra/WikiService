/**
 *
 *  RecipeWorkbench.cc
 *  DO NOT EDIT. This file is generated by drogon_ctl
 *
 */

#include "RecipeWorkbench.h"
#include <drogon/utils/Utilities.h>
#include <string>

using namespace drogon;
using namespace drogon::orm;
using namespace drogon_model::postgres;

const std::string RecipeWorkbench::Cols::_type_id = "\"type_id\"";
const std::string RecipeWorkbench::Cols::_item_id = "\"item_id\"";
const std::vector<std::string> RecipeWorkbench::primaryKeyName = {"type_id","item_id"};
const bool RecipeWorkbench::hasPrimaryKey = true;
const std::string RecipeWorkbench::tableName = "\"recipe_workbench\"";

const std::vector<typename RecipeWorkbench::MetaData> RecipeWorkbench::metaData_={
{"type_id","int64_t","bigint",8,0,1,1},
{"item_id","int64_t","bigint",8,0,1,1}
};
const std::string &RecipeWorkbench::getColumnName(size_t index) noexcept(false)
{
    assert(index < metaData_.size());
    return metaData_[index].colName_;
}
RecipeWorkbench::RecipeWorkbench(const Row &r, const ssize_t indexOffset) noexcept
{
    if(indexOffset < 0)
    {
        if(!r["type_id"].isNull())
        {
            typeId_=std::make_shared<int64_t>(r["type_id"].as<int64_t>());
        }
        if(!r["item_id"].isNull())
        {
            itemId_=std::make_shared<int64_t>(r["item_id"].as<int64_t>());
        }
    }
    else
    {
        size_t offset = (size_t)indexOffset;
        if(offset + 2 > r.size())
        {
            LOG_FATAL << "Invalid SQL result for this model";
            return;
        }
        size_t index;
        index = offset + 0;
        if(!r[index].isNull())
        {
            typeId_=std::make_shared<int64_t>(r[index].as<int64_t>());
        }
        index = offset + 1;
        if(!r[index].isNull())
        {
            itemId_=std::make_shared<int64_t>(r[index].as<int64_t>());
        }
    }

}

RecipeWorkbench::RecipeWorkbench(const Json::Value &pJson, const std::vector<std::string> &pMasqueradingVector) noexcept(false)
{
    if(pMasqueradingVector.size() != 2)
    {
        LOG_ERROR << "Bad masquerading vector";
        return;
    }
    if(!pMasqueradingVector[0].empty() && pJson.isMember(pMasqueradingVector[0]))
    {
        dirtyFlag_[0] = true;
        if(!pJson[pMasqueradingVector[0]].isNull())
        {
            typeId_=std::make_shared<int64_t>((int64_t)pJson[pMasqueradingVector[0]].asInt64());
        }
    }
    if(!pMasqueradingVector[1].empty() && pJson.isMember(pMasqueradingVector[1]))
    {
        dirtyFlag_[1] = true;
        if(!pJson[pMasqueradingVector[1]].isNull())
        {
            itemId_=std::make_shared<int64_t>((int64_t)pJson[pMasqueradingVector[1]].asInt64());
        }
    }
}

RecipeWorkbench::RecipeWorkbench(const Json::Value &pJson) noexcept(false)
{
    if(pJson.isMember("type_id"))
    {
        dirtyFlag_[0]=true;
        if(!pJson["type_id"].isNull())
        {
            typeId_=std::make_shared<int64_t>((int64_t)pJson["type_id"].asInt64());
        }
    }
    if(pJson.isMember("item_id"))
    {
        dirtyFlag_[1]=true;
        if(!pJson["item_id"].isNull())
        {
            itemId_=std::make_shared<int64_t>((int64_t)pJson["item_id"].asInt64());
        }
    }
}

void RecipeWorkbench::updateByMasqueradedJson(const Json::Value &pJson,
                                            const std::vector<std::string> &pMasqueradingVector) noexcept(false)
{
    if(pMasqueradingVector.size() != 2)
    {
        LOG_ERROR << "Bad masquerading vector";
        return;
    }
    if(!pMasqueradingVector[0].empty() && pJson.isMember(pMasqueradingVector[0]))
    {
        if(!pJson[pMasqueradingVector[0]].isNull())
        {
            typeId_=std::make_shared<int64_t>((int64_t)pJson[pMasqueradingVector[0]].asInt64());
        }
    }
    if(!pMasqueradingVector[1].empty() && pJson.isMember(pMasqueradingVector[1]))
    {
        if(!pJson[pMasqueradingVector[1]].isNull())
        {
            itemId_=std::make_shared<int64_t>((int64_t)pJson[pMasqueradingVector[1]].asInt64());
        }
    }
}

void RecipeWorkbench::updateByJson(const Json::Value &pJson) noexcept(false)
{
    if(pJson.isMember("type_id"))
    {
        if(!pJson["type_id"].isNull())
        {
            typeId_=std::make_shared<int64_t>((int64_t)pJson["type_id"].asInt64());
        }
    }
    if(pJson.isMember("item_id"))
    {
        if(!pJson["item_id"].isNull())
        {
            itemId_=std::make_shared<int64_t>((int64_t)pJson["item_id"].asInt64());
        }
    }
}

const int64_t &RecipeWorkbench::getValueOfTypeId() const noexcept
{
    static const int64_t defaultValue = int64_t();
    if(typeId_)
        return *typeId_;
    return defaultValue;
}
const std::shared_ptr<int64_t> &RecipeWorkbench::getTypeId() const noexcept
{
    return typeId_;
}
void RecipeWorkbench::setTypeId(const int64_t &pTypeId) noexcept
{
    typeId_ = std::make_shared<int64_t>(pTypeId);
    dirtyFlag_[0] = true;
}

const int64_t &RecipeWorkbench::getValueOfItemId() const noexcept
{
    static const int64_t defaultValue = int64_t();
    if(itemId_)
        return *itemId_;
    return defaultValue;
}
const std::shared_ptr<int64_t> &RecipeWorkbench::getItemId() const noexcept
{
    return itemId_;
}
void RecipeWorkbench::setItemId(const int64_t &pItemId) noexcept
{
    itemId_ = std::make_shared<int64_t>(pItemId);
    dirtyFlag_[1] = true;
}

void RecipeWorkbench::updateId(const uint64_t id)
{
}
typename RecipeWorkbench::PrimaryKeyType RecipeWorkbench::getPrimaryKey() const
{
    return std::make_tuple(*typeId_,*itemId_);
}

const std::vector<std::string> &RecipeWorkbench::insertColumns() noexcept
{
    static const std::vector<std::string> inCols={
        "type_id",
        "item_id"
    };
    return inCols;
}

void RecipeWorkbench::outputArgs(drogon::orm::internal::SqlBinder &binder) const
{
    if(dirtyFlag_[0])
    {
        if(getTypeId())
        {
            binder << getValueOfTypeId();
        }
        else
        {
            binder << nullptr;
        }
    }
    if(dirtyFlag_[1])
    {
        if(getItemId())
        {
            binder << getValueOfItemId();
        }
        else
        {
            binder << nullptr;
        }
    }
}

const std::vector<std::string> RecipeWorkbench::updateColumns() const
{
    std::vector<std::string> ret;
    if(dirtyFlag_[0])
    {
        ret.push_back(getColumnName(0));
    }
    if(dirtyFlag_[1])
    {
        ret.push_back(getColumnName(1));
    }
    return ret;
}

void RecipeWorkbench::updateArgs(drogon::orm::internal::SqlBinder &binder) const
{
    if(dirtyFlag_[0])
    {
        if(getTypeId())
        {
            binder << getValueOfTypeId();
        }
        else
        {
            binder << nullptr;
        }
    }
    if(dirtyFlag_[1])
    {
        if(getItemId())
        {
            binder << getValueOfItemId();
        }
        else
        {
            binder << nullptr;
        }
    }
}
Json::Value RecipeWorkbench::toJson() const
{
    Json::Value ret;
    if(getTypeId())
    {
        ret["type_id"]=(Json::Int64)getValueOfTypeId();
    }
    else
    {
        ret["type_id"]=Json::Value();
    }
    if(getItemId())
    {
        ret["item_id"]=(Json::Int64)getValueOfItemId();
    }
    else
    {
        ret["item_id"]=Json::Value();
    }
    return ret;
}

Json::Value RecipeWorkbench::toMasqueradedJson(
    const std::vector<std::string> &pMasqueradingVector) const
{
    Json::Value ret;
    if(pMasqueradingVector.size() == 2)
    {
        if(!pMasqueradingVector[0].empty())
        {
            if(getTypeId())
            {
                ret[pMasqueradingVector[0]]=(Json::Int64)getValueOfTypeId();
            }
            else
            {
                ret[pMasqueradingVector[0]]=Json::Value();
            }
        }
        if(!pMasqueradingVector[1].empty())
        {
            if(getItemId())
            {
                ret[pMasqueradingVector[1]]=(Json::Int64)getValueOfItemId();
            }
            else
            {
                ret[pMasqueradingVector[1]]=Json::Value();
            }
        }
        return ret;
    }
    LOG_ERROR << "Masquerade failed";
    if(getTypeId())
    {
        ret["type_id"]=(Json::Int64)getValueOfTypeId();
    }
    else
    {
        ret["type_id"]=Json::Value();
    }
    if(getItemId())
    {
        ret["item_id"]=(Json::Int64)getValueOfItemId();
    }
    else
    {
        ret["item_id"]=Json::Value();
    }
    return ret;
}

bool RecipeWorkbench::validateJsonForCreation(const Json::Value &pJson, std::string &err)
{
    if(pJson.isMember("type_id"))
    {
        if(!validJsonOfField(0, "type_id", pJson["type_id"], err, true))
            return false;
    }
    else
    {
        err="The type_id column cannot be null";
        return false;
    }
    if(pJson.isMember("item_id"))
    {
        if(!validJsonOfField(1, "item_id", pJson["item_id"], err, true))
            return false;
    }
    else
    {
        err="The item_id column cannot be null";
        return false;
    }
    return true;
}
bool RecipeWorkbench::validateMasqueradedJsonForCreation(const Json::Value &pJson,
                                                         const std::vector<std::string> &pMasqueradingVector,
                                                         std::string &err)
{
    if(pMasqueradingVector.size() != 2)
    {
        err = "Bad masquerading vector";
        return false;
    }
    try {
      if(!pMasqueradingVector[0].empty())
      {
          if(pJson.isMember(pMasqueradingVector[0]))
          {
              if(!validJsonOfField(0, pMasqueradingVector[0], pJson[pMasqueradingVector[0]], err, true))
                  return false;
          }
        else
        {
            err="The " + pMasqueradingVector[0] + " column cannot be null";
            return false;
        }
      }
      if(!pMasqueradingVector[1].empty())
      {
          if(pJson.isMember(pMasqueradingVector[1]))
          {
              if(!validJsonOfField(1, pMasqueradingVector[1], pJson[pMasqueradingVector[1]], err, true))
                  return false;
          }
        else
        {
            err="The " + pMasqueradingVector[1] + " column cannot be null";
            return false;
        }
      }
    }
    catch(const Json::LogicError &e)
    {
      err = e.what();
      return false;
    }
    return true;
}
bool RecipeWorkbench::validateJsonForUpdate(const Json::Value &pJson, std::string &err)
{
    if(pJson.isMember("type_id"))
    {
        if(!validJsonOfField(0, "type_id", pJson["type_id"], err, false))
            return false;
    }
    else
    {
        err = "The value of primary key must be set in the json object for update";
        return false;
    }
    if(pJson.isMember("item_id"))
    {
        if(!validJsonOfField(1, "item_id", pJson["item_id"], err, false))
            return false;
    }
    else
    {
        err = "The value of primary key must be set in the json object for update";
        return false;
    }
    return true;
}
bool RecipeWorkbench::validateMasqueradedJsonForUpdate(const Json::Value &pJson,
                                                       const std::vector<std::string> &pMasqueradingVector,
                                                       std::string &err)
{
    if(pMasqueradingVector.size() != 2)
    {
        err = "Bad masquerading vector";
        return false;
    }
    try {
      if(!pMasqueradingVector[0].empty() && pJson.isMember(pMasqueradingVector[0]))
      {
          if(!validJsonOfField(0, pMasqueradingVector[0], pJson[pMasqueradingVector[0]], err, false))
              return false;
      }
    else
    {
        err = "The value of primary key must be set in the json object for update";
        return false;
    }
      if(!pMasqueradingVector[1].empty() && pJson.isMember(pMasqueradingVector[1]))
      {
          if(!validJsonOfField(1, pMasqueradingVector[1], pJson[pMasqueradingVector[1]], err, false))
              return false;
      }
    else
    {
        err = "The value of primary key must be set in the json object for update";
        return false;
    }
    }
    catch(const Json::LogicError &e)
    {
      err = e.what();
      return false;
    }
    return true;
}
bool RecipeWorkbench::validJsonOfField(size_t index,
                                       const std::string &fieldName,
                                       const Json::Value &pJson,
                                       std::string &err,
                                       bool isForCreation)
{
    switch(index)
    {
        case 0:
            if(pJson.isNull())
            {
                err="The " + fieldName + " column cannot be null";
                return false;
            }
            if(!pJson.isInt64())
            {
                err="Type error in the "+fieldName+" field";
                return false;
            }
            break;
        case 1:
            if(pJson.isNull())
            {
                err="The " + fieldName + " column cannot be null";
                return false;
            }
            if(!pJson.isInt64())
            {
                err="Type error in the "+fieldName+" field";
                return false;
            }
            break;
        default:
            err="Internal error in the server";
            return false;
    }
    return true;
}
